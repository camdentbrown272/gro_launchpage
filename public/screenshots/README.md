Three screenshots, exactly these filenames:

    feed.png      the feed — a post, used beside the headline AND in the gallery
    profile.png   a profile — what someone is chasing / building
    end.png       the end-of-feed screen

Straight off the phone is fine. The page draws the phone frame around them, so
export without a device frame. They are cropped to 9:19.5 to fit, so nothing
important should sit hard against the top or bottom edge.

Any file that is missing renders a labelled placeholder frame instead, so the
page never looks broken mid-swap.
