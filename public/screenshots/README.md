Drop app screenshots here (PNG, ideally 1170×2532 — a 9:19.5 export).

Then point at them in `src/config.ts`:

    { src: '/screenshots/feed.png', ... }

Any slot left as `src: null` renders a labeled placeholder frame instead.
