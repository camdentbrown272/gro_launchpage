// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // TODO: set this to your real domain once you have one.
  // It's used for canonical URLs and the social share image.
  site: 'https://gro.app',
});
